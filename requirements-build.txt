# Pinned build/runtime closure for the macOS universal2 builds.
#
# Why this file exists: beets hard-declares numba + scipy (which pull llvmlite),
# but its code never imports them and we exclude them from the Nuitka freeze.
# On x86_64 macOS there are NO numba/llvmlite wheels, so a normal install tries
# to build llvmlite from source (needs LLVM) and fails. Installing this list
# with `--no-deps` sidesteps that: numba/scipy/llvmlite are simply never
# resolved, and both macOS arches get an identical module set (clean lipo merge).
#
# Maintenance: this is the full transitive closure MINUS {numba, scipy, llvmlite},
# resolved for Python 3.12 (the CI build version). Regenerate when bumping beets
# or the Python build version. The Python 3.13-only stdlib backports
# (audioop-lts, standard-aifc/chunk/sunau) are intentionally absent — on 3.12
# those modules are still in the standard library. Also intentionally absent:
# the `pathlib` PyPI backport (beets-vgmdb declares it; it breaks modern Python).
# NOTE: httpx2/httpcore2/truststore come from pylast 7.1.0's new HTTP stack.
# Intentionally EXCLUDED: ordered-set (not a Nuitka 4.1.3 dependency at all) and
# zstandard (only a Nuitka `onefile`/`app` extra — we build --standalone, so it's
# unused; if present it gets dragged into the bundle via urllib3's optional zstd
# support, wasting ~1.2 MB). The Windows/Linux builds (pip install -e .[build])
# never pull them either, so leaving them out keeps all platforms identical.
anyio==4.14.2
appdirs==1.4.4
audioread==3.1.0
beautifulsoup4==4.15.0
beetcamp==0.24.3
beets-filetote==1.3.6
beets-vgmdb==1.3.5
beets==2.13.0
blinker==1.9.0
certifi==2026.7.22
charset-normalizer==3.4.9
click==8.4.2
colorama==0.4.6
confuse==2.2.1
filetype==1.2.0
flask-cors==6.0.5
flask==3.1.3
h11==0.16.0
httpcore2==2.9.1
httpcore==1.0.9
httpx2==2.9.1
httpx==0.28.1
idna==3.18
ifaddr==0.2.0
itsdangerous==2.2.0
jellyfish==1.2.1
jinja2==3.1.6
langdetect==1.0.9
lap==0.5.13
lxml==6.1.1
markupsafe==3.0.3
mediafile==0.17.0
mutagen==1.48.1
Nuitka==4.1.3
numpy==2.5.1
oauthlib==3.3.1
packaging==26.2
pillow==12.3.0
platformdirs==4.11.0
pyacoustid==1.3.1
pycountry==26.2.16
pylast==7.1.0
pyrate-limiter==4.4.0
python-dateutil==2.9.0.post0
python-mpd2==3.1.1
python3-discogs-client==2.9
pyxdg==0.28
PyYAML==6.0.3
rarfile==4.4
requests-oauthlib==2.0.0
requests-ratelimiter==0.10.0
requests==2.34.2
six==1.17.0
soco==0.31.1
soupsieve==2.9.1
titlecase==2.4.1
truststore==0.10.4
typing_extensions==4.16.0
Unidecode==1.4.0
urllib3==2.7.0
werkzeug==3.1.8
xmltodict==1.0.4
