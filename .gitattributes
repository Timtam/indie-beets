# Shell scripts must keep LF endings: they run inside Linux containers (the
# clean-room smoke), where a CRLF shebang/`set` line makes bash fail with
# "set: pipefail: invalid option name". Editing them from Windows silently
# rewrote them to CRLF once and broke both Linux CI jobs.
*.sh text eol=lf
